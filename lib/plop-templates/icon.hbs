/* GENERATED FILE */
import { forwardRef } from "react";
import {
  IconWeight,
  IconProps,
  PaintFunction,
  renderPathForWeight,
  RenderFunction,
} from "../lib";
import { IconBase } from "../lib/IconBase";

const pathsByWeight = new Map<IconWeight, PaintFunction>([
  {{#each weightMaps}}
  {{#if (isFill key (pascalCase ../name)) }}
  ["{{key}}", () => (
  {{else}}
  ["{{key}}", (color: string) => (
  {{/if}}
      <>
        {{{content}}}
      </>
    )
  ],
  {{/each}}
]);

const {{ camelCase name }}Definition: RenderFunction = (weight: IconWeight, color: string) => renderPathForWeight(weight, color, pathsByWeight);

const {{ pascalCase name }} = forwardRef<SVGSVGElement, IconProps>((props, ref) => (
  <IconBase ref={ref} {...props} renderPath={ {{ camelCase name }}Definition } />
));

{{ pascalCase name }}.displayName = "{{ pascalCase name }}";

export { {{ pascalCase name }}, {{ camelCase name }}Definition };
